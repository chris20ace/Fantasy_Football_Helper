<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application android:label="Sunday Desk" android:theme="@style/AppTheme"
        android:allowBackup="false" android:usesCleartextTraffic="false"
        android:supportsRtl="true">
        <activity android:name=".MainActivity" android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- A private process gives ESPN its own WebView cookie/data directory. -->
        <activity android:name=".EspnSignInActivity" android:exported="false"
            android:process=":espn_signin" android:excludeFromRecents="true"
            android:windowSoftInputMode="adjustResize" />
    </application>
</manifest>
